<% if (showsDebugMagicLinks && flash.debugMagicLink) { %>
  <div role="alert" class="border-b border-amber-300 bg-amber-100 px-6 py-3 text-sm text-amber-900">
    <p class="font-medium">No mail is sent in the prototype. Use this sign-in link:</p>
    <a class="break-all underline" href="<%= flash.debugMagicLink %>"><%= flash.debugMagicLink %></a>
  </div>
<% } %>
