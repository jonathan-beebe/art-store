# Work Journal

## Next ticket numbers

- RSRCH: 1
- DSGN: 1
- ARCH: 1
- FEAT: 18
- IMPRV: 9
- MAINT: 1
- A11Y: 1
- RFCTR: 5
- BUG: 7

## Log

- 2026-08-23:09:43:27 — RFCTR-003 — done: four form parsers return `{ ok: true; value } | { ok: false; errors }` (`parseCheckoutForm` also hands back the trimmed `entered` form for the 422 re-render), the `xErrors`/`parseX` pairs are gone and each route branches on `result.ok`; one price grammar — `isDollarAmount` and `parseDollars` share `DOLLAR_AMOUNT_PATTERN`, so `$249` and `1,234.00` are accepted on both sides and `12.345` refused on both, and `parseDollars`' throw is no longer reachable from a route; `Cents` branded `number & { readonly __brand: 'Cents' }` with `cents`/`parseDollars`/`centsFromColumn` as the only constructors and money columns typed `ColumnType<Cents, number, number>` (writes still take the plain integer, which is what left the seed and fixture inserts alone), new `subtractCents`/`negateCents` because `-a` on a branded number is a `number`; one `toCount` in `app/db/count.ts` with `countAll`/`sum` typed `string | number | bigint` as the driver actually returns them; `AppConfig` is `z.output` of a transformed `environmentSchema`; `as const satisfies` on 13 lookup tables; unreachable guards in `decline-reason.ts`/`listing-stock.ts` replaced by exhaustiveness the compiler enforces, with the three `as never` tests dropped; `seller/listing-form.ts` parses the multipart body with one zod schema (the file part passes through by reference — `toBuffer` reads the part it is a method of); `POST /cart/:slug` now 400s on a quantity that is not a quantity instead of silently reading it as one. `TransitionError` stays and its doc comment now states the line it marks (user-triggerable refusal vs programmer error); converting the transitions to result unions is left for IMPRV-002's pass over the same route bodies. Docs still name `listingDraftErrors`/`faqDraftErrors`/`isCheckoutComplete`/`type Cents = number` — outside territory, flagged for FEAT-017. Isolated worktree at HEAD: 1461 → 1470 pass, 0 fail, `npm run check` exit 0 at 99.34/96.46/99.27.
- 2026-08-23:09:38:32 — IMPRV-003 — done: new `app/logging.ts` (`loggingOptions` — `genReqId` from `x-request-id`/generated, `requestIdHeader`, `requestIdLogLabel: 'requestId'`, a `req` serializer redacting `seller_id`/`customer_id`/`admin_id`/`flash` cookie values; `createCliLogger` for the CLIs) wired into `app.ts` via two small anchored edits (`events` decorator work was landing concurrently) plus a new optional `loggerStream` on `AppDependencies`/`buildTestApp` for tests; nine business events (`order.paid`/`order.declined`, `payout.run`, `magic_link.requested`/`consumed`/`refused`, `moderation.listing_removed`/`listing_removal_lifted`/`customer_blocked`/`customer_block_lifted`) logged from the route shell after the action result is applied, never the magic-link token/URL; the four CLI entrypoints (`run-payouts`, `drain-outbox`, `migrate`, `seed`) moved off `console.log` onto an injectable pino logger, one structured line per unit of work plus a summary, `process.exitCode = 1` and a logged `err` on failure; `pino` added to `package.json`/`package-lock.json` dependencies (was only fastify's transitive dep) pinned to the version already resolved; `'no-console': 'error'` in eslint with no override, since every remaining `console.*` call was already confined to the four entrypoints and all four now log through the logger. `order.placed`/`fulfillment.shipped` left for a follow-up — `checkout.ts`/`carts.ts`/`seller/routes/*` are RFCTR-003's territory this cycle. `npm run check` green at exit 0, 1492 pass, 0 fail, coverage 99.57/96.60/99.55 (shared tree, other tickets landing concurrently — not all of that delta is this ticket's).
- 2026-08-23:09:30:33 — FEAT-016 — done: live unread badge over SSE — `app/plugins/events.ts` decorates `app.events` (typed `node:events` `EventEmitter<{ changed: []; closing: [] }>`), a root `onResponse` hook fires `changed` after any request that wrote (non-GET/HEAD, < 400) and a `preClose` hook fires `closing`; `unreadEventStream` is a web `ReadableStream` sending `retry: 3000`, then the actor's count now, then the count each time it moves, unsubscribing on client `AbortSignal`, on `closing`, and on a failing count; `unreadEventsRoute(actorType)` serves `GET <prefix>/events` as `text/event-stream` inside each site's existing identity guard (`shop/storefront.ts`, `seller/index.ts`, `admin/console.ts`). 21 dependency-free lines of `public/app.js` (one `<script defer>` per layout) write the count into the badge, which the layouts now always render as an `empty:hidden` span carrying `data-unread-messages` and the number only when there is one. Deliberately did **not** emit from `notify`/`postMessage` through an optional `ActionContext.events`: both run inside `runInTransaction`, so an emit there fires before commit, and the two of three message routes that would have had to pass `events` are another worker's territory — a payload-free `changed` plus a per-stream re-read of that stream's own actor is settled-after-commit, reaches all three sites, also catches a thread being read, and cannot leak another actor's count. `docs/review.md:88`'s "zero `<script>` tags across the 57 templates" rewritten truthfully; `security-headers.ts`'s policy comment likewise (CSP unchanged — `script-src 'self'` already allowed it). Shared tree does not import (three workers mid-flight), so `npm run check` was verified in an isolated worktree at HEAD with only this ticket's files: 1448 → 1456 pass, 0 fail, exit 0, coverage 99.59/96.70/99.54.
- 2026-08-23:09:22:04 — FEAT-016 — started
- 2026-08-23:09:14:07 — IMPRV-003 — started
- 2026-08-23:09:13:34 — RFCTR-003 — started
- 2026-08-23:09:11:27 — IMPRV-001 — done: one root `setErrorHandler` (`plugins/error-pages.ts`) maps a thrown `ZodError` to 400 and any error carrying a 4xx `statusCode` to that status, logs everything else through `request.log.error({ err })` and renders a generic 500 — each in the layout of the site the request landed on, plain text where there is none (`/health` stays JSON); a `setNotFoundHandler` per site plus a `site.all('/*')` for the prefixed portals, because `@fastify/static`'s root `/*` otherwise claims every unmatched GET; `addSiteRender` returns a `SitePageRenderer` so a 404 handed over by `callNotFound` on a reply that never entered a site still renders that site's page; new `not-found`/`error` templates per site; `renderNotFound`, `sellerNotFound`, the two private admin `notFound` helpers and the inlined admin expressions are all `reply.callNotFound()` now. The twenty `.parse` sites are deliberately untouched (IMPRV-002 owns them) — the verified reproduction `POST /login` with a duplicated `email` field answers 400 text/html. 1431 → 1448 pass, 0 fail.
- 2026-08-23:09:10:17 — FEAT-015 — done: transactional outbox — new `outbox_messages` table; `outboxNotificationDelivery`/`outboxMagicLinkDelivery` enqueue a row **inside the caller's transaction** (both ports' `deliver` now takes a `DeliveryContext` of `{ db, clock }`, and `NotificationDelivery` moved out of `app/core` where a db handle did not belong); `notify` wraps its inbox row and its delivery in one `runInTransaction` and defaults to the outbox, `sendMagicLink` likewise, so a rolled-back business transaction leaves neither row; `drainOutbox` renders each pending row through the pure `renderMailMessage` (RFC-5322, CRLF, header-injection folded) and writes `<OUTBOX_DIR>/<id>.eml` then stamps `delivered_at`, outside any transaction, reachable as `npm run outbox` / `make outbox` / `POST /admin/outbox/drain`; `/admin/outbox` lists and `/admin/outbox/:id` shows the rendered message with its link clickable, which is the demo sign-in path once `MAGIC_LINK_DELIVERY=outbox` turns the debug alert off; `mailMagicLinkDelivery` and `not-implemented-error.ts` deleted, `MAGIC_LINK_DELIVERY` is now `flash|outbox`, new `OUTBOX_DIR`. No SMTP. Dropped the ticket's `NOTIFICATION_DELIVERY` env var deliberately — every route builds its own ActionContext literal and the shop/seller route files are another ticket's territory this cycle, so the knob could not have reached the call sites (reasoned in the ticket). `docs/architecture.md` and `docs/identity.md` still describe the deleted mail hook — outside territory, flagged for FEAT-017. 1385 → 1428 pass, 0 fail, `npm run check` exit 0 at 99.54/96.69/99.71 verified in an isolated worktree (the shared tree carries IMPRV-001 WIP).
- 2026-08-23:09:08:59 — BUG-005 — done: pay route (`markAwaitingPayment` + `awaitsCard` branch + `finalizeOrder`) now one `runInTransaction`; delivery confirmation drops its stale route-level gate and catches `confirmDelivered`'s `TransitionError` into a flash + redirect like the seller ship route; `changeListingStatus` now reads the active removal itself and refuses `for_sale` under it for any caller, not just the seller route; opening a listing question and posting the first message run as one transaction so a refused first message (empty body or a blocked customer) leaves no conversation row; `orders.ts`'s `isCancellable` fast-404 gained a comment naming `transitionOrder` as the authority. 4 new tests (2 messages.ts, 1 change-listing-status action, 1 seller listings route); `npm run check` green at 1446 pass, 0 fail, 99.54/96.74/99.81 coverage.
- 2026-08-23:08:53:00 — FEAT-015 — started
- 2026-08-23:08:47:26 — IMPRV-001 — started
- 2026-08-23:08:46:11 — BUG-005 — started
- 2026-08-23:08:45:11 — BUG-006 — done: `NODE_ENV` parses into `environment` and production refuses to boot without `COOKIE_SECRET` or with `MAGIC_LINK_DELIVERY=flash` (the delivery that prints sign-in links into the page); `PUBLIC_URL` (reduced to an origin) is what magic links are built from, so the `Host` header decides nothing where one is set; `TRUST_PROXY` drives Fastify's `trustProxy`; `secureCookies` (production or an https public url) puts `Secure` on the identity and flash cookies; `showsDebugMagicLinks` reaches every page through site-render and guards the debug partial; new `plugins/security-headers.ts` adds nosniff / DENY / strict-origin-when-cross-origin / a `default-src 'self'` CSP to every response with no new dependency. Kept the `flash` default and no `SECURE_COOKIES` knob (both reasoned in the ticket). 1362 → 1382 pass, 0 fail.
- 2026-08-23:08:43:35 — IMPRV-006 — done: shared world-builder — `app/test/commerce-world.ts`'s `APPROVED_CARD`/`DECLINED_CARD`/`SHIPPING_ADDRESS` are the single source, `storefront-fixtures.ts` re-exports them under their existing names and `seller/test-fixtures.ts`'s disagreeing `DEFAULT_SHIPPING` and inline card literal are gone; `migrator.test.ts`/`database.test.ts` use `t.after` cleanup, no more leaked temp dirs; `fulfillments.test.ts`'s four 404 tests now assert the fulfillment row and ledger entries didn't move; `faqs.ts` and `order-payments.ts` route branch coverage 64%/78.57% → 100%/100% with new authorization/validation-branch tests; core edge cases pinned in `money`/`fee`/`payout-period`/`listing-draft`/`listing-slug`/`email-address` tests (`dollarsInputValue`'s negative-sign drop pinned, not fixed — noted); new `find-storefront-listings.test.ts` (10 tests, search/medium/pagination); `flashSchema` exported from `plugins/flash.ts` and its two `JSON.parse` test casts replaced with `flashSchema.parse`. Scope narrowed against the ticket's file list per the worker brief (no `checkout.test.ts`, `seed-order-history.ts`, admin routes, or seller/shop queries beyond `find-storefront-listings`); `commerce-world.ts`'s direct-insert `createListing` deliberately left unreconciled with the other two fixtures' lifecycle-walking builders (~35 files outside territory import it). 1312 → 1382 pass, 0 fail; suite branch coverage 95.65% → 96.60%.
- 2026-08-23:08:41:12 — FEAT-013 — done: three-stage Dockerfile (`dev` unchanged for `make up`, `build` runs npm ci + Tailwind once, `runtime` is npm ci --omit=dev, USER node, NODE_ENV=production, declared storage/uploads volumes, HEALTHCHECK against /health, no entrypoint, no seeding) on node:24.19.0-bookworm-slim; `make image`/`make run-image`; new `.dockerignore`; `engines: ">=24.15"`; truthful seed comments in entrypoint.sh and the Makefile; README Deployment section. Verified via `docker build`/`docker run`: node v24.19.0, uid 1000, 0 eslint packages, migrate + /health 200 + HEALTHCHECK healthy, dev target unchanged, image 289MB. `npm run check` blocked by concurrent BUG-006 WIP outside this ticket's territory (see ticket's Working section) — not fixed here.
- 2026-08-23:08:38:21 — RFCTR-004 — done: 13 `Promise.all` batch loads unwrapped into sequential awaits (the dialect holds one connection, so they never overlapped); `merge-anonymous-customer.ts` rewritten against the typed Kysely builder with `merged-table-columns.ts` and its `sqlite_master`/`pragma_table_info` scan deleted; `REPOINTED_CUSTOMER_TABLES` moved to `app/actions/customers/` as `readonly (keyof Database)[]`; notification rows parse once into `{recipientType, recipientId}` via `parseNotificationRow` and `notify` inserts through an explicit switch with delivery moved out of the transaction; the four single-statement actions call their write directly; `make fresh` stops the app container before the delete and starts it after; `STOREFRONT_STATUSES`/`BROWSABLE_STATUSES` exported from core and driving both storefront queries' `where`
- 2026-08-23:08:38:00 — FEAT-013 — started
- 2026-08-23:08:33:14 — BUG-006 — started
- 2026-08-23:08:29:00 — RFCTR-004 — started
- 2026-08-23:08:26:33 — RFCTR-002 — done: `shopName`/`customerName` now used at every seller/customer display site (three admin queries, two admin page titles); `faqPrefill`, `activityWindow`, `isSentBy`, and participant naming (`counterpartName`/`senderName`/`ABSENT_COUNTERPART`) moved into core with their own tests; one `openSupportConversation` action replaces the duplicated inline "first admin by id" query on both /support routes; `ConversationOpening` is a discriminated union per kind with `missingConversationParts` and its `TypeError` deleted; `ConversationActor.isBlocked` is now per-variant; `REMOVED_FILTERS`/`MAGIC_LINK_STATUSES` gained as-const arrays; `planCustomerMerge` returns `favoritesToMove`/`favoritesToDrop` so `applyFavorites` only issues statements. Skipped the storefront-visibility sub-item per the orchestrator's scope change — another ticket owns it.
- 2026-08-23:08:26:00 — FEAT-011 — started
- 2026-08-23:08:45:00 — FEAT-011 — done: GET /health (core evaluateHealth + plugins/health.ts) answering 200/503 with database + migrations checks and a draining state, SIGINT/SIGTERM now log, drain via a `draining` decorator, await close(), set exitCode on failure, and force-exit after 10s if close hangs, docker-compose healthcheck against /health via node fetch (no curl in image)
- 2026-08-23:08:22:03 — IMPRV-006 — started
- 2026-08-23:08:20:02 — IMPRV-005 — done: `formatCents` now uses `Intl.NumberFormat` (byte-identical output), `app/core/shop/day-label.ts` gained `dateLabel`/`dateTimeLabel`/`timestampLabel`/`dayFromReportKey` on module-level `Intl.DateTimeFormat` instances (locale + UTC explicit), `app/sites/seller/format.ts` and `admin/page.ts`'s `formatMoment` are now thin wrappers over it with names/output unchanged, the `test-fixtures.ts` buyer email uses `randomUUID()` instead of `Date.now()`/`Math.random()`, and `server.ts`/`db/migrate.ts`/`db/seed.ts`/`cli/run-payouts.ts` each export a guarded `main(argv, env)` with a new test exercising `run-payouts`' `main` against a real temp-file database; `placeholder-image.ts`'s `Buffer` base64 stays — `Uint8Array.prototype.toBase64` is unflagged-unavailable on the installed Node 24.12
- 2026-08-23:08:17:10 — FEAT-014 — done: `.github/workflows/node.yml` runs typecheck, lint, and the coverage-gated suite on Node 24 for pushes/PRs touching `prototype/node`, then uploads `coverage/lcov.info`; `npm run check` now runs `typecheck && lint && coverage`, thresholds raised 90/80 → 95/90, new `test:ci` script pairs `spec`+`lcov` reporters for CI's readable output plus the artifact
- 2026-08-23:08:11:38 — FEAT-014 — started
- 2026-08-23:08:08:40 — IMPRV-005 — started
- 2026-08-23:08:08:16 — IMPRV-004 — done: CHECK constraints on all eleven status/kind columns generated from their core `as const` arrays (added to the original create-table migrations, since SQLite has no ALTER TABLE ADD CONSTRAINT — existing databases need `make fresh`), a schema-fidelity test checking every table's columns and nullability against `pragma_table_info`, `migrateDown`/`pendingMigrations` plus a down-to-zero-and-back migration cycle test, and `page_view_counts.site`/`listings.quantity`/`listings.status`/`fulfillments.status`/`page_view_counts.count` carrying their narrow/`Generated` types
- 2026-08-23:08:07:30 — BUG-003 — done: checkout plans placement in core and refuses a stale cart by name instead of 500ing, placement and the charge commit as one transaction, and the add-to-cart gate reads the listing in the transaction that writes the line
- 2026-08-23:07:53:17 — BUG-003 — started
- 2026-08-23:07:59:56 — RFCTR-002 — started
- 2026-08-23:07:57:57 — BUG-004 — done: consuming a magic link and claiming the actor now commit or roll back together, the nine auth/customer actions take ActionContext and join the caller's transaction, claim returns are read back from the database, and cookie-id parsing lives once in parseActorId
- 2026-08-23:07:52:39 — IMPRV-004 — started
- 2026-08-23:07:52:22 — BUG-004 — started
- 2026-08-23:07:51:32 — BUG-002 — done: uploaded listing images are now verified by sniffing PNG/JPEG/GIF/WebP magic bytes (SVG and anything else refused with a field error, browser filename/Content-Type decide nothing), multipart capped at 5 MB with an oversized upload re-rendering the form instead of a JSON 413, uploads served with X-Content-Type-Options: nosniff from a dedicated /uploads prefix, and the uploads directory now comes from config.uploadsDir (mkdtemp per test)
- 2026-08-23:07:50:47 — FEAT-012 — done: SQLite now comes from node:sqlite behind an owned Kysely dialect; better-sqlite3 and the compiler layer are gone
- 2026-08-23:07:48:30 — RFCTR-001 — done: escrow balance folding, one-payout-per-period, and as-of day parse consolidated into core
- 2026-08-23:07:37:45 — FEAT-012 — started
- 2026-08-23:07:36:40 — RFCTR-001 — started
- 2026-08-23:07:36:30 — BUG-002 — started
- 2026-08-23:22:15:24 — FEAT-017 — defined: final validation and documentation refresh
- 2026-08-23:22:15:23 — IMPRV-008 — defined: plugins are Fastify plugins, type-aware lint, printed route table
- 2026-08-23:22:15:22 — IMPRV-007 — defined: templates interpolate finished view models
- 2026-08-23:22:15:21 — IMPRV-006 — defined: test gaps and fixture hygiene
- 2026-08-23:22:15:20 — IMPRV-005 — defined: platform idioms — Intl formatting, one date formatter, web-standard base64, testable entrypoints
- 2026-08-23:22:15:19 — FEAT-016 — defined: live unread badge over Server-Sent Events
- 2026-08-23:22:15:18 — FEAT-015 — defined: transactional outbox — notifications and magic links delivered after commit
- 2026-08-23:22:15:17 — RFCTR-004 — defined: data-layer idioms — sequential awaits, typed merge, parsed notifications
- 2026-08-23:22:15:16 — RFCTR-003 — defined: forms parse into complete values or explicit errors
- 2026-08-23:22:15:15 — RFCTR-002 — defined: messaging, naming, and listing decisions move into core
- 2026-08-23:22:15:14 — RFCTR-001 — defined: escrow and payout decisions move into core
- 2026-08-23:22:15:13 — IMPRV-004 — defined: status unions enforced by the database and row types checked against migrations
- 2026-08-23:22:15:12 — IMPRV-003 — defined: structured logging through the built-in logger
- 2026-08-23:22:15:11 — IMPRV-002 — defined: validation declared on routes, handlers receive typed input
- 2026-08-23:22:15:10 — IMPRV-001 — defined: errors and unmatched routes render the site's HTML, never JSON
- 2026-08-23:22:15:09 — FEAT-014 — defined: CI runs check and coverage on Node 24
- 2026-08-23:22:15:08 — FEAT-013 — defined: a production image that runs anywhere
- 2026-08-23:22:15:07 — FEAT-012 — defined: SQLite through node:sqlite behind a Kysely dialect owned by the app
- 2026-08-23:22:15:06 — FEAT-011 — defined: real readiness endpoint and draining shutdown
- 2026-08-23:22:15:05 — BUG-006 — defined: production-unsafe defaults — debug magic-link alert, cookie secret, insecure cookies, Host-derived links
- 2026-08-23:22:15:04 — BUG-005 — defined: four routes read, branch, then write across separate transactions
- 2026-08-23:22:15:03 — BUG-004 — defined: magic-link sign-in and identity claims run outside any transaction
- 2026-08-23:22:15:02 — BUG-003 — defined: checkout can 500 or sell a removed listing when the cart is stale
- 2026-08-23:22:15:01 — BUG-002 — defined: uploaded listing images trust the browser's filename and content type
- 2026-08-22:21:59:58 — FEAT-010 — done: final validation — clean first run from an empty tree in 27s, a curl walk of 116 checks with no 500, a six-test smoke walk from the seller's sign-in to the weekly payout, the removal rule and `dollarsInputValue` moved into `app/core`, one `statusLabel` and one `parseIdParam` for all three sites, nine dead exports deleted, `runWeeklyPayout` no longer collides on an already-settled period, plus `docs/review.md` and a rewritten `README.md`
- 2026-08-22:21:35:03 — FEAT-009 — done: docs — architecture.md corrected against the code (versions, app/plugins, site queries, commerce-schema, cli, delivery ports, customerStanding under core/moderation, the order state machine, the smoke test), plus identity/orders/escrow/messaging/admin/data-model/ontology docs and a README index, and `make docs-check` rendering all 19 Mermaid blocks through minlag/mermaid-cli
- 2026-08-22:21:19:35 — BUG-001 — done: no admin write route answers 500 for a bodiless post — `formBody(request)` reads an absent body as an empty form at every throwing `zod.parse` site across all four sites, and `moderationRoute` safeParses and answers 400 when the form does not hold together
- 2026-08-22:21:14:19 — FEAT-010 — started
- 2026-08-22:21:11:55 — BUG-001 — started
- 2026-08-22:21:09:04 — FEAT-009 — started
- 2026-08-22:21:08:15 — BUG-001 — defined: admin lift routes answer 500 when the request has no body
- 2026-08-22:21:05:34 — FEAT-007 — done: messaging center — one `conversations` table across four kinds with `messages` and `listing_faqs`, pure access/find-or-open/unread-fold/validation in app/core/messaging, six actions plus the shared inbox and thread reads, inboxes and threads on all three sites, ask-a-question on the listing page with FAQ publish/edit/unpublish, support and fulfillment threads, in-app notification per message, unread badges in every layout, seeded demo threads and one published FAQ
- 2026-08-22:20:37:31 — FEAT-007 — started
- 2026-08-22:20:30:42 — FEAT-006 — done: admin site — dashboard, sellers/customers, filterable listings/orders/fulfillments/payouts/ledger tables, accounting that reconciles with the ledger, site stats, the four moderation writes behind one route factory, the weekly payout run, everything behind requireAdmin, and a root onResponse page-view rollup
- 2026-08-22:20:27:27 — FEAT-005 — done: customer storefront — paged/searchable grid, listing page with view events and an empty FAQ section, favorites, cart, checkout that pays a verified customer and emails a guest a link redirecting to the pay page, decline-with-retry and stock return, orders with per-seller fulfillments, cancel, confirm-delivery, account notifications; search/paging/checkout-form ported pure into app/core/shop
- 2026-08-23:01:27:28 — FEAT-004 — done: seller portal — dashboard, listings (index/new/edit/show, multipart image upload, removal-aware status buttons), fulfillments grouped by status with mark-shipped, earnings (held/available/paid out, sold-goods, payout history, no payout button), notifications inbox; four pure reports ported into app/core/reports; requireSeller everywhere, cross-seller and non-numeric ids 404 on read and write
- 2026-08-22:20:06:00 — FEAT-008 — done: seed data and demo reset — 4 verified sellers with shops and a 29-listing catalog across six media (one under temporary removal), casey@example.com with favorites/views/cart/orders in paid/shipped/delivered, a blocked customer, 3 anonymous browsers, 14 days of page-view counts, all driven through the FEAT-003 actions on a frozen clock
- 2026-08-22:20:03:52 — FEAT-008 — started
- 2026-08-22:19:54:09 — FEAT-005 — started
- 2026-08-22:19:53:20 — FEAT-006 — started
- 2026-08-23:00:52:28 — FEAT-004 — started
- 2026-08-22:19:45:49 — FEAT-002 — done: magic-link identity for sellers, customers, and admins — one delivery port with a flash and a mail hook, seeded-only admins, anonymous customer per storefront request, and an anonymous-to-verified merge folded by a pure plan
- 2026-08-22:19:44:16 — FEAT-003 — done: commerce domain core — 15 tables, pure core for listings/cart/orders/payments/escrow/notifications/moderation/analytics, actions over { db, clock } in transactions, weekly payouts CLI, lifecycle/decline/cancel tests
- 2026-08-22:19:14:58 — FEAT-003 — started
- 2026-08-22:19:14:11 — FEAT-002 — started
- 2026-08-22:19:08:42 — FEAT-001 — done: dockerized Node 24 + Fastify foundation — three site plugins with layouts and the debug alert, buildApp composition root, Kysely migrator, node:test sidecars, typecheck/lint/coverage gates, Tailwind CLI
- 2026-08-22:18:38:04 — FEAT-001 — started
- 2026-08-22:20:40:00 — FEAT-010 — defined: final validation — clean first run, smoke test, coverage, review, README
- 2026-08-22:20:40:00 — FEAT-009 — defined: docs — drift check, feature docs, data model, ontology, render check
- 2026-08-22:20:40:00 — FEAT-008 — defined: seed data and demo reset
- 2026-08-22:20:40:00 — FEAT-007 — defined: messaging center with listing questions and FAQ publishing
- 2026-08-22:20:40:00 — FEAT-006 — defined: admin site — users, orders, accounting, site stats, moderation
- 2026-08-22:20:40:00 — FEAT-005 — defined: customer storefront — browse, favorite, cart, guest checkout, pay, orders
- 2026-08-22:20:40:00 — FEAT-004 — defined: seller portal — listings, activity, fulfillments, earnings, notifications
- 2026-08-22:20:40:00 — FEAT-003 — defined: commerce domain core
- 2026-08-22:20:40:00 — FEAT-002 — defined: magic-link identity for sellers, customers, admins with anonymous merge
- 2026-08-22:20:40:00 — FEAT-001 — defined: dockerized Node 24 + Fastify foundation with sidecar node:test and Tailwind
