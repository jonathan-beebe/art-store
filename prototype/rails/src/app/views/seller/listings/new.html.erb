<% content_for :title, "New listing — Art Store seller" %>

<h1 class="text-lg font-semibold">New listing</h1>

<%= render "seller/listings/form",
      url: seller_listings_path,
      method: :post,
      submit_label: "Save as draft",
      fields: @fields,
      errors: @errors %>
