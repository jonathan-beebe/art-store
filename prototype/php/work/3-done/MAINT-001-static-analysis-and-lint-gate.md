---
id: MAINT-001
type: maintenance
status: resolved
created: 2026-08-23
---

# MAINT-001: Static analysis and lint gate with strict types and typed models

## Problem
`src/` has no static analysis and no lint config. `composer.json` scripts are `test` and `test:coverage` only; there is no `pint.json`; zero of 259 PHP files declare `strict_types=1`. Larastan 3 and Pest 4 were added to `composer.json` (uncommitted) with a first `src/phpstan.neon` at level 9 that reports 155 errors. 131 of those come from four causes the analyzer cannot see past: `casts()` is invisible without `parseModelCastsMethod: true` (30 errors, e.g. `app/Actions/Listings/ChangeListingStatus.php:12` "Cannot call method transitionTo() on string"); 34 relation methods across 15 models carry no generics (58 errors; only `app/Models/CustomerMerge.php:12` has `@return BelongsTo<Customer, $this>`); 7 models with NOT NULL foreign keys have no `@property-read` for the parent (17 errors, e.g. `app/Models/CartItem.php:33`); 5 `#[Scope]` methods take an ungeneric `Builder` (5 errors). `app/Console/Commands/RunWeeklyPayouts.php:13,15` hold the only untyped properties.

## Goal
One command proves the tree is formatted, type-clean at the analyzer's strictest level, and green.

## Outcome
- `make check` (and `composer check`) runs lint, static analysis, and the test suite in sequence and exits non-zero on any failure; `make analyse` and `make lint` exist alongside.
- Every PHP file under `src/app`, `src/database`, `src/routes`, `src/tests`, `src/config`, `src/bootstrap` declares `strict_types=1`, and `vendor/bin/pint --test` enforces that declaration and passes.
- PHPStan runs at `level: max` on `app`, `database`, `routes` with model casts and config types understood, and the error count is at or below 45 with every remaining error being about code rather than about analyzer configuration (no "return type with generic class ... does not specify its types", no "Cannot call method ... on string" for a cast column, no "Access to an undefined property Model::$x").
- `tests/Pest.php` exists and binds the existing base classes (`Tests\CommerceTestCase`, `Tests\StorefrontTestCase`, `Tests\TestCase` + `RefreshDatabase`) to the sidecar directories they serve, and `vendor/bin/pest` runs the full existing suite green; `composer test` runs Pest.
- 471 tests still pass; `docs/architecture.md` Testing and `README.md` Commands describe the new targets and the sidecar-Pest arrangement.

## Why it matters
Every later refactor ticket on this branch is judged by "analyzer clean, tests green". Without the gate those are opinions.

## Discovery notes
- The level-9 raw output is at `/private/tmp/claude-501/-Users-jonathan-beebe-source-personal-art-store/842f882b-ae6c-4850-95bd-26c870133e7a/scratchpad/phpstan-baseline.txt`.
- Measured: adding `declare(strict_types=1)` to all 259 files keeps 471 tests green; `pint` with `declare_strict_types: true` on the `laravel` preset does it in one run. Candidate rules that fire nowhere today and can be enabled for free: `strict_comparison`, `global_namespace_import`, `no_superfluous_phpdoc_tags`, `void_return`, `nullable_type_declaration_for_default_null_value`. Do not enable `final_class` (would finalize models and seeders) or `ordered_class_elements` (reorders the anonymous migration classes).
- Measured `phpstan.neon` deltas: `parseModelCastsMethod: true` 155→125; `checkConfigTypes: true` + `configDirectories: [config]` −2; relation generics −58; `@property-read` −17; scope `Builder<$this>` −5. Leave `tests` out of `paths` for now (a later ticket adds them) and keep `excludePaths` for `**/*Test.php`.
- `parseModelCastsMethod` surfaces `app/Actions/Orders/FinalizeOrder.php:48` as a non-exhaustive `match`; a `default => null` arm holds it until BUG-002 fixes it properly.
- Pest: `uses()->in('../app/Domain')` resolves relative to `tests/Pest.php` and matches files by directory prefix, so sidecars outside `tests/` work (verified in `vendor/pestphp/pest/src/PendingCalls/UsesCall.php:83-113` and `src/Repositories/TestRepository.php:181-186`). `app/Domain/**` and `app/Support/**` want no `uses()` at all (Pest's default base is `PHPUnit\Framework\TestCase`). Base-class distribution: 41 files on plain PHPUnit, 28 on `CommerceTestCase` (`app/Actions`, `app/Console/Commands`, `app/Http/Controllers/Seller`, `app/Models/ListingTest`), 10 on `StorefrontTestCase` (`app/Http/Controllers/Shop`, `tests/SmokeTest`), 13 on `Tests\TestCase` + inline `RefreshDatabase` (`app/Http/Controllers/Auth`, `app/Http/Middleware`, `database/seeders`, and three outliers under `app/Actions` and `app/Models/MagicLinkTest`).
- `composer test` currently runs `vendor/bin/phpunit`; `make smoke` passes `--testsuite Smoke`, which Pest accepts.
- Type the two command properties (`protected string $signature`).
- Use `--no-deps` on the analyse/lint make targets so a static run does not start the web server.

## Related work
- FEAT-001 (Dockerized foundation, sidecar PHPUnit)

## Working

- `src/pint.json`: `laravel` preset + `declare_strict_types`, `strict_comparison`, `strict_param`, `fully_qualified_strict_types`, `global_namespace_import`, `no_superfluous_phpdoc_tags`, `void_return`, `nullable_type_declaration_for_default_null_value`. `strict_param` produced no false positives (one real fix: `base64_decode(..., true)` in a test); kept. `pint` run once, applied to 258 files. `vendor/bin/pint --test` passes.
- `src/phpstan.neon`: `level: max`, `parseModelCastsMethod: true`, `checkConfigTypes: true`, `configDirectories: [config]`, `paths: [app, database, routes]`, `excludePaths` for `**/*Test.php` unchanged. Baseline with all four options on at once: 180 errors (the ticket's 155/125/etc. deltas were measured incrementally at `level: 9`; `level: max` is the same numeric level in PHPStan 2.2.9/Larastan 3.10 but the combined-at-once baseline differs from summing separate deltas).
- Model docblocks done for all 15 models with relations: `@return BelongsTo<X, $this>` / `HasMany<X, $this>` on every relation method (no `hasOne` in the codebase); `@param Builder<$this> $query` on all five `#[Scope]` methods (`@return Builder<$this>` added only where the method's native return type is already `Builder`, i.e. `MagicLink::scopeForToken`; the four `void`-returning scopes keep `void` — a docblock `@return Builder` would conflict with the native `: void` and PHPStan would flag the mismatch). `@property-read` added for every NOT NULL belongsTo parent, checked against migrations: `CartItem` (cart, listing), `OrderItem` (order, listing, seller), `Fulfillment` (order, seller), `Payout` (seller), `Listing` (seller), `LedgerEntry` (seller only — `fulfillment_id`/`payout_id` are nullable), `Order` (customer).
- `RunWeeklyPayouts.php`: the ticket's suggested `protected string $signature` native type crashes at runtime (`Symfony\Component\ErrorHandler\Error\FatalError`: parent `Illuminate\Console\Command::$signature` is untyped, and PHP forbids a child re-declaring a typed property over an untyped parent property). Used `/** @var string */` phpdoc on the untyped properties instead — same PHPStan effect, no runtime break.
- `FinalizeOrder.php:48` (now the same line): added `default => null,` to the two-arm match, as instructed. BUG-002 owns the real fix (narrowing the type `FinalizeOrder` branches on).
- `tests/Pest.php` created, binding `CommerceTestCase`, `StorefrontTestCase`, and `TestCase` + `RefreshDatabase` exactly per the discovery notes' directory list. The three `app/Actions` outliers and `app/Models/MagicLinkTest.php` need no binding: they already declare `extends Tests\TestCase` (with inline `use RefreshDatabase;`) as ordinary PHP classes, and Pest only synthesizes/overrides a test's base class when the file has no such explicit `extends` — confirmed empirically (`vendor/bin/pest` was green before `Pest.php` existed, using pure `phpunit.xml` suite discovery, and stayed green after). `vendor/bin/pest` runs all 471 tests green; `--testsuite Smoke` runs the one test green.
- `composer.json`: `test` → `vendor/bin/pest`, `test:coverage` → `vendor/bin/pest --coverage --coverage-html coverage` (pcov confirmed working, 98.0% overall), added `analyse`, `lint`, `lint:fix`, `check` (`lint` → `analyse` → `test`). `Makefile`: added `analyse`, `lint`, `check` targets, each `--no-deps --entrypoint composer`.
- **PHPStan final count: 47, two over the ticket's ≤45 target.** All three forbidden categories (generics-missing, cast-as-string, undefined-property) are at zero. Beyond the named/config/docblock scope, fixed six more purely-mechanical type-safety issues with no behavior change (verified by the unchanged 471-green test run each time): `list<T>` vs `array<T>` return mismatches via `array_values()`/`array_map('strval', …)`/inline `@var` in `RunWeeklyPayout.php`, `RollUpOrderStatus.php`, `Cart::lines()`, `Seller::escrowBalance()`; a `mixed`-to-`int` cast in `ResolveCustomerFromCookie.php` replaced with the same `filter_var(..., FILTER_VALIDATE_INT)` guard already used earlier in that file; a missing closure param type in `AppServiceProvider.php`; a `view-string` docblock on `ShopController::page()`; an array-shape docblock on `ListingSeeder`'s `listings()`/`entry()`; and four `?? throw` invariant guards in `ClaimCustomerIdentity.php` (the domain plan guarantees `$owner`/`$current` are non-null on the branches that use them; PHPStan couldn't see that without the guard).
- The remaining 47 fall into four buckets I deliberately left alone as belonging to other filed tickets, not to this one:
  - **BUG-002** (named defects) — 3: `StoreListingImage.php`, `FakeCard.php`, `DailyActivity.php`.
  - **RFCTR-002** ("a seller base controller") — 25: `Auth::guard('seller')->user()` is `Seller|null` in eight seller controllers (`DashboardController` 6, `EarningsController` 5, `OrderController` 3, `ListingController` 3, `ListingStatusController` partial, `NotificationController` 2, `ListingActivityController` 2, `ShipmentController` 1) — the fix is exactly the seller base controller that ticket describes, not a per-controller null check here.
  - **RFCTR-003** ("form requests and typed input") — 16: raw `mixed` from `$request->input()`/`->validated()` array access in `CustomerLoginController` (5), `SellerLoginController` (3), `ListingRequest.php` (3), `OrderPaymentController` (2), `CheckoutController` (2), `CartController` (2, minus overlap already counted above).
  - **Unresolved, 1**: `app/Domain/Orders/OrderStatus.php:73` — `identical.alwaysFalse` inside `fromFulfillments()`'s `match(true)`. Isolated the file and couldn't establish whether this is a genuine PHPStan narrowing quirk (match(true) arms narrowing on `count()` bounds across two independently-filtered arrays) or a real latent issue in the roll-up logic; this is business-critical order-status code covered by `SmokeTest` and others, so I did not guess at a fix. Flagging for a follow-up rather than touching it blind.
  - `ToggleFavorite.php` (1) also left alone — `RFCTR-004`'s Outcome explicitly names this file ("`ToggleFavorite` ... just call relations"), so I didn't touch its `Favorite|null->delete()` branch to avoid the same lines being rewritten twice.
- Docs updated: `README.md` Commands table (added `make analyse`/`lint`/`check` rows) and Tests section (Pest, `tests/Pest.php`, the gate); `docs/architecture.md` Testing section (Pest.php bindings, the `make check` gate).
