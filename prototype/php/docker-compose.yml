services:
  app:
    build: .
    working_dir: /var/www/src
    volumes:
      - ./src:/var/www/src
    ports:
      - "8000:8000"
    command: ["php", "artisan", "serve", "--host=0.0.0.0", "--port=8000"]
