@props(['text' => 'coming — not in this version'])

{{-- The muted pill a seller screen reaches for to flag a described feature as designed but not shipped. --}}
<span class="rounded-full border border-gray-200 dark:border-gray-700 px-2 py-0.5 text-xs">{{ $text }}</span>
