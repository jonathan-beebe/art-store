; pcov instruments every line it touches, which costs CPU on every request.
; It stays disabled here so the `php -S` workers behind `artisan serve` run
; at full speed; composer's test scripts turn it back on with -d for the
; one process that collects coverage.
pcov.enabled=0
