<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
>
    <testsuites>
        <testsuite name="App">
            <directory suffix="Test.php">app</directory>
            <directory suffix="Test.php">routes</directory>
            <directory suffix="Test.php">database</directory>
            <!-- The layer rules. Named for what it holds rather than with the
                 `Test.php` suffix the directories match, so it is listed. -->
            <file>tests/Arch.php</file>
        </testsuite>
        <!-- The one end-to-end walk. Its own suite so `make smoke` can run it
             alone; the default run includes it. -->
        <testsuite name="Smoke">
            <directory suffix="Test.php">tests</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
        <exclude>
            <directory suffix="Test.php">app</directory>
        </exclude>
    </source>
    <php>
        <env name="APP_ENV" value="testing"/>
        <env name="APP_MAINTENANCE_DRIVER" value="file"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="BROADCAST_CONNECTION" value="null"/>
        <env name="CACHE_STORE" value="array"/>
        <env name="DB_CONNECTION" value="sqlite"/>
        <env name="DB_DATABASE" value=":memory:"/>
        <env name="DB_URL" value=""/>
        <!-- The suite's own output shares this stream; the tests that read log
             lines capture them through the formatter instead. -->
        <env name="LOG_CHANNEL" value="null"/>
        <!-- docs/logging.md: the store's own tests build one against a temp
             file or :memory: explicitly. Left on, the whole suite would write
             a store nothing reads. -->
        <env name="LOG_DATABASE_FILE" value="off"/>
        <!-- config/database.php: the analytics connection's own in-memory
             database, isolated from the commerce connection's. -->
        <env name="ANALYTICS_DATABASE_FILE" value=":memory:"/>
        <!-- config/analytics.php: off by default, so an ordinary test never
             has orders:sweep prune rows out from under it. SweepOrdersTest
             overrides this per test, the way it does log_store.retention_days. -->
        <env name="ANALYTICS_RETENTION_DAYS" value="off"/>
        <env name="MAIL_MAILER" value="array"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <env name="SESSION_DRIVER" value="array"/>
        <env name="PULSE_ENABLED" value="false"/>
        <env name="TELESCOPE_ENABLED" value="false"/>
        <env name="NIGHTWATCH_ENABLED" value="false"/>
    </php>
</phpunit>
